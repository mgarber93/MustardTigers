const database = require('../database');
const expect = require('chai').expect;

xdescribe('User schema', function() {
  it('inserts new users', function() {
  });

  it('does not allow duplicate users', function() {
  });
});
